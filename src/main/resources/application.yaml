spring:
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1
    locations: classpath:db/migration
  cloud:
    function:
      definition: completePayment
    stream:
      defaultBinder: kafka
      kafka:
        binder:
          brokers:
            - ${SPRING_KAFKA:localhost:29092}
      bindings:
        completePayment-in-0:
          destination: formula-saved
          group: payment-service
        sendCommunication-out-0:
          destination: send-communication
  rabbitmq:
    host: "localhost"
    port: 5672
    username: "guest"
    password: "guest"
  config:
    import: "optional:configserver:http://localhost:8071/"
  profiles:
    active:
      - "prod"
  application:
    name: payment-service
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    properties:
      jakarta:
        persistence:
          schema-generation:
            scripts:
              action: create
              create-target: target/schema.sql
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: validate
    show-sql: false
  datasource:
    url: ${SPRING_DATASOURCE_URL_PAYMENT_SERVICE:jdbc:postgresql://localhost:5434/autoecole-payment}
    username: ${SPRING_POSTGRES_USERNAME:postgres}
    password: ${SPRING_POSTGRES_PASSWORD:root}
    driver-class-name: org.postgresql.Driver



server:
  port: 3003

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true
  info:
    env:
      enabled: true
  health:
    probes:
      enabled: true
    readinessstate:
      enabled: true
    livenessstate:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}



eureka:
  instance:
    preferIpAddress: true
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/


info:
  app:
    name: "payment-service"
    description: "payment service microservice"

logging:
  file:
    name: ${LOG_FILE_NAME:logs/payment-service.log}
  structured:
    format:
      console: ecs
      file: ecs
    ecs:
      service:
        name: ${spring.application.name}
        environment: ${SPRING_PROFILES_ACTIVE:local}
  level:
    root: INFO
